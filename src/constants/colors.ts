const black = '#000000';
const white = '#ffffff';

const text = {
  gray: '#a8aabd',
};

const background = {
  darkBlue: '#262a59',
};

const colors = { black, white, text, background };

export default colors;
